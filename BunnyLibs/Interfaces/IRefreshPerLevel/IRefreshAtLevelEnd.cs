namespace BunnyLibs
{
	internal interface IRefreshAtLevelEnd
    {
        // Avoid name collisions
        void RefreshAtLevelEnd(Agent agent);
    }

    // Import: P_ExitPoint
    // Subclasses: Homesickly, Homesickless
}